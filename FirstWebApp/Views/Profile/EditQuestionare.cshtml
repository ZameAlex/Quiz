@model DataClasses.Models.Quiz
@{
    ViewBag.Title = "EditQuiz";
}

<div class="container">
    <h2>Quiz</h2>
    <form method="post">
        <div class="form-group">
            <label for="name">Name:</label>
            <input type="text" class="form-control" id="name" name="name" placeholder="Enter quiz name" value="@Model.Name">
        </div>
        <button type="submit" class="btn btn-primary" formaction=@Url.Action("EditQuestionare","Profile")>Save Name</button>
    </form>
</div>

